# keep this pinned. 1.97.1 handles the lockfile and edition 2024 crates.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy", "rust-src"]
targets = ["x86_64-pc-windows-msvc"]
profile = "minimal"
